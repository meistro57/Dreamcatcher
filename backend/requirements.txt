fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
redis==5.0.1
python-multipart==0.0.6
websockets==12.0
# Pinned above 2.6.0 to satisfy safety's dependency floor
pydantic==2.9.2
pydantic-settings==2.1.0
email-validator==2.2.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
PyJWT==2.8.0
python-dotenv==1.0.0
alembic==1.12.1

# AI Services
anthropic==0.7.8
openai==1.3.5

# Audio Processing
# NOTE: `openai-whisper` pulls very large torch dependencies. It is optional at runtime:
# the app degrades gracefully if unavailable (voice transcription disabled).
# Install it manually when needed:
#   pip install openai-whisper==20231117
numpy==1.26.4
webrtcvad==2.0.10

# Utilities
aiofiles==23.2.1
httpx==0.25.2
aiohttp==3.9.1
websockets==12.0
jinja2==3.1.2
celery==5.3.4
apscheduler==3.10.4

# Vector Database & Embeddings
pgvector==0.2.4
# `sentence-transformers`/`scikit-learn` are optional for semantic search model loading.
# The service falls back when unavailable.
# Install manually to enable full semantic features:
#   pip install sentence-transformers==2.2.2 scikit-learn==1.3.2

# Development
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
black==23.11.0
flake8==6.1.0
mypy==1.7.1
bandit==1.7.5
safety==3.6.2
